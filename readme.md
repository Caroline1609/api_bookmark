# A simple (and dirty) REST API in PHP

Launch:

```bash
git clone https://gitlab.com/miwoteam/basic-rest.git
cd basic-rest

docker compose up --build -d
```

Then visit http://127.10.10.20/ in your browser (or any HTTP client like Bruno, curl or Postman)